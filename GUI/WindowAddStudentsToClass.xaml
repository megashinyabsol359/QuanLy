<Window x:Class="GUI.WindowAddStudentsToClass"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GUI"
        mc:Ignorable="d"
         Loaded="Window_Loaded"
        Title="Thêm học sinh vào Lớp" Height="600" Width="1000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <ContentControl  Name="Content" Content="{Binding}"></ContentControl>
        <Grid Grid.Row="1">
            <Button x:Name="AddStudent" Content="Thêm vào lớp" HorizontalAlignment="Left" Margin="687,0,0,0" VerticalAlignment="Top" Width="141" Click="AddStudent_Click">
            </Button>
            <Button x:Name="Cancle" Content="Hủy" HorizontalAlignment="Left" Margin="860,0,0,0" VerticalAlignment="Top" Width="116" Click="Cancle_Click" Background="#FFF47103"/>
        </Grid>
    </Grid>
</Window>
